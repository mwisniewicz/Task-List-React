import React from 'react';

const Navigation = (props) => {
    return (
        <nav>
            <ul>
                <li>Home</li>
                <li>Contact</li>
            </ul>
        </nav>
    );
};

export default Navigation;